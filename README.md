# chota_chota
Этап 1. Подготовка среды
Я установила Python (версия 3.6+).

Я установила библиотеку mcpi:

bash
pip install mcpi
Я запустила Minecraft Pi Edition или сервер с поддержкой API.

Я проверила подключение к игре и вывела тестовое сообщение:

python
from mcpi.minecraft import Minecraft
mc = Minecraft.create()
mc.postToChat("Мод про птичек загружен!")
Этап 2. Проектирование мода
Я определила виды птиц (воробей, сова, попугай) и прописала их характеристики:

скорость полёта;

высота полёта;

звуки (если API поддерживает);

поведение (дружелюбные, пугливые, агрессивные).

Я выбрала блоки для визуализации птиц:

шерсть разных цветов;

цветные стёкла;

декоративные блоки.

Я продумала игровые механики:

спавн птиц в биомах (лес, джунгли, горы);

взаимодействие с игроком (кормление, приручение);

гнездование и размножение;

сезонные миграции.

Этап 3. Реализация базовых функций
Я создала класс Bird для описания птицы:

python
class Bird:
    def __init__(self, x, y, z, species, color_block):
        self.pos = (x, y, z)
        self.species = species
        self.color_block = color_block
Я реализовала спавн птиц:

настроила случайное размещение в подходящих биомах;

задала ограничение по высоте (y∈[64,128]);

организовала группы — стаи по 3–5 птиц.

Я добавила движение птиц:

запрограммировала случайные перемещения в радиусе 10 блоков;

реализовала анимацию «полёта» через смену блоков;

добавила логику избегания препятствий.

Я реализовала отображение птиц в мире:

python
def spawn_bird(bird):
    mc.setBlock(bird.pos[0], bird.pos[1], bird.pos[2], bird.color_block)
Этап 4. Взаимодействие с игроком
Я настроила отслеживание позиции игрока:

python
player_pos = mc.player.getTilePos()
Я добавила реакцию птиц на приближение игрока:

пугливые птицы улетают, если расстояние меньше 5 блоков (if distance<5);

дружелюбные птицы подлетают ближе.

Я реализовала механику кормления:

если игрок держит семена (wheat_seeds), птицы следуют за ним;

при клике правой кнопкой мыши птица приручается.

Я создала гнёзда:

разместила гнёзда в деревьях и на крышах;

запрограммировала возвращение приручённых птиц к гнезду;

добавила шанс появления птенца раз в 5 минут.

Этап 5. Игровые механики и улучшения
Я добавила звуки (если поддерживается API):

чириканье при кормлении;

крик хищной птицы при атаке.

Я реализовала сезонные изменения:

зимой птицы мигрируют в тёплые биомы;

весной строят гнёзда активнее.

Я создала систему достижений:

«Орнитолог» — приручить 5 видов;

«Птичья семья» — вырастить 3 птенцов.

Я добавила декоративные элементы:

кормушки (блоки с текстурой зерна);

скворечники на деревьях.

Этап 6. Тестирование
Я проверила спавн:

убедилась, что птицы появляются только в подходящих биомах;

проверила, что стаи не перекрывают друг друга.

Я протестировала взаимодействие:

убедилась, что кормление работает с семенами;

проверила, что приручение меняет поведение птицы.

Я убедилась, что птицы не застревают в блоках.

Я проверила производительность:

убедилась, что мод не вызывает лагов при 20+ птицах;

проверила отсутствие утечек памяти при долгом запуске.

Я запустила мод на сервере с несколькими игроками и протестировала его работу.

Этап 7. Финальные штрихи
Я оптимизировала код:

использовала setBlocks для стай;

кэшировала координаты биомов.

Я создала конфигурационный файл (config.json):

внесла настройки спавна;

прописала параметры поведения птиц;

добавила опции включения/выключения звуков.

Я написала инструкцию для пользователей:

описала процесс установки мода;

изложила правила взаимодействия с птицами;

составила список видов птиц и их особенностей.

Я упаковала проект:

включила скрипт Python;

добавила папку textures (если есть кастомные блоки);

создала файл README.md с подробным описанием.


